{
  "name": "road-nirman-monorepo",
  "private": true,
  "workspaces": [
    "web-dashboard"
  ],
  "scripts": {
    "dev": "npm run dev --workspace web-dashboard",
    "build": "npm run build --workspace web-dashboard",
    "start": "npm run start --workspace web-dashboard",
    "lint": "npm run lint --workspace web-dashboard",
    "typecheck": "npm run typecheck --workspace web-dashboard",
    "vercel-build": "npm run build --workspace web-dashboard"
  }
}
